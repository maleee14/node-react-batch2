/**
 * ES6 atau EcmaScript 6 atau EcmaScript 2015 merupakan fitur bahasa pada
pemrograman Javascript modern atau biasa di sebut next gen javascript. lalu
kenapa kita harus mempelajari ES6, itu karena library maupun framework JS
terkini banyak menggunakan sintaks-sintaks ES6.

setelah EcmaScript 6 ini banyak fitur-fitur yang dapat digunakan untuk
mengoptimalkan
 */

/**
 * berikut ini daftar fitur ES6:
● Arrows Function
● Classes
● Enchanced object literals
● Template strings
● Destructuring
● Default + rest + spread
● Let + const
● Iterators + for..of
● Generators
 */

/**
 * let dan const merupakan statement untuk mendefinisikan variable sama seperti
var hanya saja terdapat perbedaan diantara let + const dan var.

let hampir sama seperti var hanya saja jika dalam satu block kode ada sebuah let
yang sudah di definisikan kita tidak dapat mendefinisikan kembali kecuali kita
membuat block kode baru di dalamnya misal seperti menambahkan if.
sedangkan const sendiri bersifat tidak bisa di rubah seperti sebuah konstanta
dalam matematika. var sendiri masih bisa digunakan untuk menjaga compability
dengan versi sebelumnya
 */

/**
 * template literal atau biasa di sebut template string merupakan fitur ES6 yang
memungkinkan kita menyusun string dengan rapi dengan menggunakan tanda
petik terbalik dan ${variabelnya}.

contoh sintaks:
let word = “example”
let result = `this is ${word}`
 */

/**
 * Enhanced object literals merupakan fitur ES6 yang memungkinkan kita untuk
menyederhanakan sebuah object, dimana biasanya kita selalu menulis property
dan value, tetapi jika terdapat kondisi ada variabel yang namanya sama dengan
property maka kita bisa assign hal tersebut sebagai value tetapi dengan hanya
menulis property nya saja
 */

/**
 * Destructuring merupakan ekspresi javascript yang memungkinkan untuk
membagi atau memecah nilai dari sebuah array atau objek ke dalam variabel
yang berbeda
 */

/**
 * Rest Parameters dan Spread Operator di lambangkan dengan simbol yang sama
yaitu "..."
 */

/**
 * Rest Parameter ini berguna untuk menggabungkan semua paramater pada
function ke dalam array. Dengan menggunakan Rest Parameter ini dapat
membantu kita mendefinisikan function dengan rapi serta memberikan
parameter yang tidak terbatas pada sebuah function.
 */

/**
 * Spread Operator digunakan untuk membagi elemen array atau properti pada
objek, sehingga elemen array dapat ditambahkan/dimasukan ke dalam array
baru
 */